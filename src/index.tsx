import ReactDOM from 'react-dom/client';
import App from 'shell/app/Root';
import 'shared/styles/index.css';
import 'shared/styles/main.css';

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
